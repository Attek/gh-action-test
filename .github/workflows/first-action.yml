name: First Workflow
on: workflow_dispatch # manualy trigger
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: Print greating
        run: echo "Hello World!"
      - name: Print goodbye
        run: |
          echo "Done - bye!"
          echo "env: ${TRIVY_REPOSITORY}"
